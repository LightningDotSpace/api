# Server Configuration
PORT=3002

# RSK Configuration
# IMPORTANT: RSK_RPC_URL - Production should use dedicated RPC node
# Public node has rate limits and is less reliable
RSK_RPC_URL=https://public-node.rsk.co

# CRITICAL: RSK_PRIVATE_KEY - Wallet for RBTC swaps
# ⚠️ NEVER use the Hardhat test key (0xac0974...)!
# Generate new key with: openssl rand -hex 32
# Ensure wallet has enough RBTC for swaps + gas fees
RSK_PRIVATE_KEY=your_rsk_private_key_here

# EtherSwap contract address on RSK Mainnet (Chain ID 30)
ETHERSWAP_ADDRESS=0x3d9cc5780CA1db78760ad3D35458509178A85A4A

# LND Configuration
# LND_HOST - Lightning node address (host:port)
LND_HOST=localhost:10009

# LND credentials (mounted from Docker container)
# These paths work with Docker volume mount setup
LND_MACAROON_PATH=/tmp/lnd-files/admin.macaroon
LND_TLS_CERT_PATH=/tmp/lnd-files/tls.cert

# Database Configuration
# SQLite database for swap state tracking
# Relative or absolute paths supported
DB_PATH=./rsk-swaps.db
